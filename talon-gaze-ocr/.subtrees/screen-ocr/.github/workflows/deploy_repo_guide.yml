name: Publish documentation generated by repo-guide

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: wolfmanstout/deploy-repo-guide@v1
        with:
          gemini-key: ${{ secrets.LLM_GEMINI_KEY }}
