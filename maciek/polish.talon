not mode: sleep
-
# Now I understand that it can't be done that way: we can't use rephrase because the web speech can the beat on on samples it has to be recording itself
# ^polish [<phrase>]$:
    # user.polish_single_phrase(phrase or "")
    
