tag: user.mouse_grid_showing
-
<number>:
    user.grid_narrow(number)
grid off:
    user.grid_close()

grid reset:
    user.grid_reset()

#to do: doesn't do anything
#grid back:
#    user.grid_go_back()
